import 'package:flutter/material.dart';

const mainColor = Color(0xff100B20);
const kTranstionDuration = Duration(microseconds: 100);

class Style {
  static const titleMidum =
      TextStyle(fontSize: 18, fontWeight: FontWeight.w600);
}
